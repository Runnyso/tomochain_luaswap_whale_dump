# TomoChain — LuaSwap Whale Dump Tracker

Detects when someone removes **$2M+ liquidity** from LuaSwap (biggest DEX on TomoChain) in a single transaction.

TomoChain is fast and cheap — perfect for Vietnamese degens and whales.

## Run

```bash
python tomochain_luaswap_whale_dump.py
